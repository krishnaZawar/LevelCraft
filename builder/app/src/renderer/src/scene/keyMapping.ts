// Hand-copied from utils/input/mapping.json until GET /input/schema exists.
// A smoke-test subset only — replace with the real registry, don't extend it.
export const KEY_CODE_TO_NAME: Record<string, string> = {
  KeyW: 'KEY_W',
  KeyA: 'KEY_A',
  KeyS: 'KEY_S',
  KeyD: 'KEY_D',
  Space: 'KEY_SPACE',
  ArrowUp: 'KEY_UP',
  ArrowDown: 'KEY_DOWN',
  ArrowLeft: 'KEY_LEFT',
  ArrowRight: 'KEY_RIGHT'
}
