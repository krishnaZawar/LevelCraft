# Not auto-loaded — reference for running editor/app standalone.
# Normally set by the orchestrator when it spawns this app.

# URL of an already-running editor/backend. Leave unset to self-spawn.
LEVELCRAFT_BACKEND_URL=http://localhost:3000

# Port for this app's /ping server. Leave unset to bind a random port.
LEVELCRAFT_PING_PORT=
